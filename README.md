#  Stardew Valley volcano dungeon predictor

[Use it here](https://r9.pm/sdv-volcano-predictor/).

## Building

Needs a relatively modern rustc, wasm-pack and typescript (tsc). `dist.sh` builds and puts the artifacts in `dist/`.
